query GetPost($id: Int!) {
  getPost(id: $id) {
    id
    createdAt
    updatedAt
    title
    content
    artistName
    albumName
    rating
    author {
      id
      email
      firstName
      lastName
      username
      profile {
        id
        bio
        avatar
      }
    }
  }
}
