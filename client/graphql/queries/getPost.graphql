query GetPost($id: Int!) {
  getPost(id: $id) {
    id
    createdAt
    updatedAt
    title
    previewSongUrl
    artistName
    albumName
    genre
    albumImage
    rating
    author {
      id
      email
      firstName
      lastName
      username
      profile {
        id
        bio
        avatar
      }
    }
  }
}
